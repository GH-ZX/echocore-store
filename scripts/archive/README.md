# Archive

Historical one-off helpers used to live here.  
All schema/RPC migrations now live in **`../../supabase_echocore_full.sql`**.

Do not add new migrations here — append to the full SQL file instead (or apply once on prod then fold into full).
